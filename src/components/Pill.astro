<div class="pill"><slot /></div>

<style>
	.pill {
		display: flex;
		margin-top: 1.5rem;
		padding: 0.5rem 1rem;
		gap: 0.5rem;
		color: var(--gray-0);
		font-size: var(--text-md);
		font-weight: 600;
		line-height: 1.35;
		white-space: nowrap;
	}

	:global(.theme-dark) .pill{
		font-weight: 500;
	}
</style>